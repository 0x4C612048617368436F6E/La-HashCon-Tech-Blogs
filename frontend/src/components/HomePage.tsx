import React, { useState } from "react";
import Spinner from "./Spinner";
const HomePage = ()=>{
    //we begin boiler plate for home page
    const isUserLoggedIn = useState(false);
    return(
        <Spinner />

    )
}

export default HomePage;