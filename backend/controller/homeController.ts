const homeController =(req,res)=>{
    //Can send stuff. Note this is an API only
    /*
    The stuff we will ssend to the home page includes:
    1.) Send the latest or most popular posts to display
    */
    const message = {
        
    }
    res.status(200);
    res.json(message);
}